# v37 — overrun resume 2000 rpm

Статус: **EXPERIMENTAL**.

Изменение относительно предыдущего checkpoint: горячие точки кандидата `Overrun RPM Resume Threshold` подняты примерно с 1024 rpm до 2000 rpm при ECT 80/90/100/110 °C.

Точные offsets и raw values находятся в `changes.csv`.

Оригинальный BIN должен храниться в этой директории после побайтной проверки размера/ID/SHA-256. Не восстанавливать BIN только из CSV и не считать реконструкцию оригинальным test artifact.
