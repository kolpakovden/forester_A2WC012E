# v38 — overrun initial enrichment candidate

Статус: **EXPERIMENTAL**.

В этой версии изменялся кандидат `Overrun initial injector enrichment (pulsewidth)` по адресу `0x58DA0`.

Рабочая интерпретация изменения: `2.8 ms → 1.0 ms` (`2800.0 → 1000.0` как raw float в используемой интерпретации).

Точная правка находится в `changes.csv`.

Оригинальный BIN должен храниться в этой директории только после побайтной проверки размера/ID/SHA-256. Не считать восстановленный по CSV файл оригинальным тестовым артефактом.
